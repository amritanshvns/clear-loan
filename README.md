# OpenLoan

OpenLoan is an offline-first, privacy-first loan calculation and simulation platform.

The project is structured as a TypeScript monorepo:

- `packages/core-engine`: Pure financial calculation engine.
- `packages/web-ui`: Browser application shell.

See the planning documents for the current product direction:

- `requirement.md`
- `design.md`
- `task.md`
